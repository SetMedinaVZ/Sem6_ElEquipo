import 'package:flutter/material.dart';

void main() {
  runApp(const HebAR());
}

class HebAR extends StatelessWidget {
  const HebAR({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
